# SIH26073 — API service dependencies
#
# Install from the repository root:
#     pip install -r api/requirements.txt
#
# Versions are the ones the frozen model was verified against. scikit-learn in
# particular should be pinned: unpickling the frozen Isolation Forest with a
# different minor version can warn or fail. If you change it, re-run the parity
# check (`python3 src/inference.py`) before trusting any prediction.

# --- inference (must match the frozen model) ---
scikit-learn==1.9.0
pandas==3.0.5
numpy==2.5.2
joblib==1.5.3

# --- API layer ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
